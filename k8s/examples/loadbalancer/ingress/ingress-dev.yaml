apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-0chan
  namespace: nullchan-dev
spec:
  ingressClassName: nginx
  rules:
  - host: 0chan.1chan.cyou
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: frontend
            port:
              number: 80
  - host: s01.1chan.cyou
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: frontend
            port:
              number: 80
